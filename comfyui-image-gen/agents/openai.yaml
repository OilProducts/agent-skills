interface:
  display_name: "ComfyUI Image Gen"
  short_description: "Direct workflow runs and orchestrated RenderSpec page pipelines"
  default_prompt: "Use $comfyui-image-gen to run either direct ComfyUI workflow JSON jobs or RenderSpec-based draft/refine/inpaint/upscale phases, and return output paths plus reproducibility artifacts."
